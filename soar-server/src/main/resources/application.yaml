management:
  tracing:
    sampling:
      probability: 1.0

spring:
  application:
    name: soar-server

  profiles:
    active: local

  # Servlet
  servlet:
    multipart:
      max-file-size: 16MB
      max-request-size: 32MB

  # Jackson — ISO-8601 for Instant/LocalDate (e.g. "2024-01-15T10:30:00Z")
  jackson:
    serialization:
      write-dates-as-timestamps: false
      fail-on-empty-beans: false
    deserialization:
      fail-on-unknown-properties: false

  # Cache
  cache:
    type: REDIS
    redis:
      time-to-live: 1h

  # JPA / Hibernate
  jpa:
    open-in-view: false # EntityManager created at @Transactional boundary, not request start
    hibernate:
      ddl-auto: validate # Flyway manages schema; Hibernate only validates entity ↔ table mapping
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  # Flyway
  flyway:
    locations: classpath:db/migration
    clean-disabled: true
    table: sys_flyway_histories

  # Disable Spring Data Redis repositories (not used, using RedisTemplate directly)
  data:
    redis:
      repositories:
        enabled: false

# Server
server:
  servlet:
    encoding:
      enabled: true
      charset: UTF-8
      force: true

# SpringDoc
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui
  default-flat-param-object: true

--- #################### Scheduler ####################

spring:
  quartz:
    job-store-type: jdbc          # lưu lịch ở qrtz_* -> bền qua restart, sẵn sàng clustered
    jdbc:
      initialize-schema: never    # Flyway là nguồn duy nhất quản schema (migration V1_1_5)
    properties:
      org:
        quartz:
          scheduler:
            instanceName: SoarScheduler
            instanceId: AUTO      # mỗi node tự sinh id riêng - bắt buộc khi clustered
          jobStore:
            driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
            isClustered: true     # node tranh khoá ở DB -> job chỉ chạy 1 lần dù nhiều node
            clusterCheckinInterval: 15000
            useProperties: false
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 10

#--- #################### MQ ####################
#
#spring:
#  # khi sender-type = rabbitmq
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
#  # khi sender-type = kafka
#  kafka:
#    bootstrap-servers: localhost:9092
#    consumer:
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#      properties:
#        spring.json.trusted.packages: "com.hdl.soar.framework.websocket.core.sender.kafka"
#    producer:
#      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# Soar custom properties

soar:
  web:
    admin-ui:
      url: http://localhost:5173
  security:
    mock-enable: false
  tenant:
    enable: true
    ignore-caches:
      - user_role_ids
      - permission_menu_ids
      - oauth_client
  captcha:
    enable: false
  user:
    init-password: "123456"

--- #################### WebSocket ####################

soar:
  websocket:
    enable: true          # mặc định true
    path: /infra/ws       # đường handshake
    sender-type: local    # local (1 node) | redis (đa node). Mặc định local.
    sender-rabbitmq:
      exchange: ${spring.application.name}-websocket-exchange
      queue: ${spring.application.name}-websocket-queue
    sender-kafka:
      topic: ${spring.application.name}-websocket
      consumer-group: ${spring.application.name}-websocket-consumer